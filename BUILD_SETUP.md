# Інструкція з налаштування середовища розробки
## Arena Combat - Windows Build Setup

---

## ⚠️ Поточна проблема

При спробі зібрати проект виникає помилка:
```
error: linking with `link.exe` failed: exit code: 1
note: you may need to install Visual Studio build tools with the "C++ build tools" workload
```

**Причина:** Rust на Windows потребує C++ компілятор для лінкування.

---

## 🛠️ Рішення: Встановити Microsoft C++ Build Tools

### Крок 1: Завантажити Build Tools

1. Перейти: https://visualstudio.microsoft.com/downloads/
2. Прокрутити вниз до **"Tools for Visual Studio"**
3. Завантажити **"Build Tools for Visual Studio 2022"** (безкоштовно)

### Крок 2: Встановити необхідні компоненти

1. Запустити інсталятор `vs_BuildTools.exe`
2. Обрати робоче навантаження: **"Desktop development with C++"**
3. Переконатися що обрано:
   - ✅ **MSVC v143 - VS 2022 C++ x64/x86 build tools** (або новіше)
   - ✅ **Windows 11 SDK** (або Windows 10 SDK)
   - ✅ **C++ CMake tools for Windows**

4. Натиснути **Install**

**Розмір:** ~5-7 ГБ
**Час встановлення:** 10-30 хвилин (залежить від інтернету)

### Крок 3: Перезапустити термінал

Після встановлення:
1. Закрити всі термінали / PowerShell / Git Bash
2. Відкрити новий термінал
3. Перевірити що Rust бачить компілятор:

```bash
rustc --version
cargo --version
```

### Крок 4: Зібрати проект

```bash
cd c:\Claude\arena_combat
cargo build
```

Перша збірка займе **2-5 хвилин** (завантаження залежностей).

---

## 🎯 Альтернатива: GNU Toolchain (швидше встановити)

Якщо не хочете встановлювати Visual Studio Build Tools, можна використати MinGW:

### 1. Встановити MinGW-w64

Завантажити: https://www.mingw-w64.org/downloads/
Або через MSYS2: https://www.msys2.org/

### 2. Переключити Rust toolchain

```bash
rustup toolchain install stable-x86_64-pc-windows-gnu
rustup default stable-x86_64-pc-windows-gnu
```

### 3. Зібрати проект

```bash
cargo build
```

**Примітка:** GNU toolchain може мати проблеми з деякими Windows-specific бібліотеками. MSVC рекомендовано для production.

---

## ✅ Перевірка налаштування

Після встановлення Build Tools:

```bash
# Перевірити Rust
rustc --version
# Очікуваний вивід: rustc 1.92.0 (або новіше)

# Перевірити Cargo
cargo --version
# Очікуваний вивід: cargo 1.92.0 (або новіше)

# Перевірити toolchain
rustup show
# Має бути: stable-x86_64-pc-windows-msvc (default)

# Зібрати проект
cd c:\Claude\arena_combat
cargo build

# Запустити
cargo run
```

**Очікуваний результат:**
- Відкривається вікно 800x600
- Заголовок "Arena Combat Prototype"
- ESC або [X] закриває вікно

---

## 🐛 Troubleshooting

### Помилка: "link.exe not found"

**Рішення:**
1. Переконатися що Build Tools встановлено
2. Перезапустити термінал
3. Перевірити PATH (має містити шлях до MSVC)

```powershell
# PowerShell
$env:PATH -split ';' | Select-String -Pattern 'Microsoft Visual Studio'
```

### Помилка: "cannot find -lwindows"

**Рішення:**
Переконатися що встановлено Windows SDK під час встановлення Build Tools.

### Збірка дуже повільна

**Перша збірка** завжди повільна (завантаження залежностей).
**Наступні збірки** будуть швидші (incremental compilation).

**Прискорити:**
```bash
# Використати linker lld (швидший)
# Додати до Cargo.toml:
[profile.dev]
linker = "lld"
```

---

## 📝 Наступні кроки після успішної збірки

1. ✅ Запустити `cargo run` - побачити вікно
2. Додати wgpu rendering (3D graphics)
3. Створити camera controller
4. Імпортувати 3D модель манекена
5. Додати WASD movement

---

## 🔗 Корисні посилання

- [Rust на Windows](https://doc.rust-lang.org/book/ch01-01-installation.html#installing-rustup-on-windows)
- [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/)
- [Cargo Book](https://doc.rust-lang.org/cargo/)

---

**Створено:** 2025-12-11
**Оновлено:** 2025-12-11

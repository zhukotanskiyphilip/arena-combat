# Методологія розробки Arena Combat
## AI-Assisted Development Protocol

**Версія:** 1.0
**Дата:** 2025-12-14
**Принцип:** "Документуй для майбутнього себе, який нічого не пам'ятає"

---

## 🎯 Основний принцип

> **Кожна сесія розробки починається з НУЛЯ.**
> Новий AI-асистент не має контексту попередніх сесій.
> Вся критична інформація МАЄ бути записана в файлах.

---

## 📋 Обов'язковий протокол кожної сесії

### 1️⃣ ПОЧАТОК СЕСІЇ: Читання контексту (5-10 хв)

**Порядок читання документів:**

```
1. README.md (arena_combat/) - швидкий огляд проекту
2. PROGRESS.md - ЩО зроблено, ЩО далі
3. arena_combat_gdd.md - філософія гри (якщо треба)
4. tech_stack_decision.md - технічні рішення (якщо треба)
5. METHODOLOGY.md - цей файл
```

**Критичні питання перед початком роботи:**
- [ ] Який поточний статус проекту?
- [ ] Яка фаза розробки?
- [ ] Що було зроблено в останній сесії?
- [ ] Які є блокери/проблеми?
- [ ] Що треба зробити далі?

### 2️⃣ РОБОТА: Документування паралельно з кодом

**ПРАВИЛО:** Кожна зміна коду → запис у PROGRESS.md

**Що документувати:**
1. **Створення нового файлу:**
   - Призначення файлу
   - Зв'язки з іншими файлами
   - Використані залежності

2. **Зміна існуючого коду:**
   - ЩО змінено
   - НАВІЩО змінено
   - ЯКІ наслідки для інших файлів

3. **Прийняття рішень:**
   - Яку проблему вирішували
   - Які альтернативи розглядалися
   - Чому обрали це рішення

4. **Проблеми/Блокери:**
   - Точний опис проблеми
   - Спроби вирішення
   - Чи вирішено? Як?

### 3️⃣ КІНЕЦЬ СЕСІЇ: Підготовка для наступного AI (10 хв)

**Чеклист перед завершенням:**

```markdown
- [ ] Оновити дату в PROGRESS.md
- [ ] Записати все виконане у Timeline
- [ ] Оновити "Наступні кроки"
- [ ] Перевірити чи працює код (cargo build/run)
- [ ] Записати невирішені проблеми
- [ ] Закомітити зміни в Git з описовим коміт-повідомленням
```

**Шаблон запису в PROGRESS.md:**

```markdown
### YYYY-MM-DD (Сесія N): [Коротка назва що було зроблено]
**Тривалість:** ~X годин
**Фаза:** Phase X - [Назва фази]

#### Виконано:
- ✅ [Завдання 1 з деталями]
- ✅ [Завдання 2 з деталями]
- ✅ [Завдання 3]

#### Технічні деталі:
- Створено файли: `src/file1.rs`, `src/file2.rs`
- Додано залежності: `crate_name = "version"`
- Зміни в архітектурі: [опис]

#### Проблеми під час сесії:
- ⚠️ [Проблема 1]
  - Спроба вирішення: [...]
  - Результат: ✅ Вирішено / ⏸️ Відкладено / ❌ Заблоковано

#### Наступні кроки (Сесія N+1):
- [ ] [Конкретне завдання 1]
- [ ] [Конкретне завдання 2]
```

---

## 📂 Структура документації проекту

### Обов'язкові файли:

| Файл | Призначення | Оновлюється |
|------|-------------|-------------|
| `README.md` | Швидкий старт, що це за проект | Рідко |
| `PROGRESS.md` | **ЖУРНАЛ УСІХ ЗМІН** | **КОЖНА СЕСІЯ** |
| `METHODOLOGY.md` | Цей файл - правила роботи | По потребі |
| `arena_combat_gdd.md` | Game Design Document | По потребі |
| `tech_stack_decision.md` | Технічні рішення | По потребі |

### Додаткові файли (створюються по потребі):

- `ARCHITECTURE.md` - архітектура коду (коли стане складною)
- `TROUBLESHOOTING.md` - часті проблеми та рішення
- `API.md` - опис публічних API модулів

---

## 💻 Стандарти коду

### Коментарі у .rs файлах

**Кожен новий файл ПОЧИНАЄТЬСЯ з header блоку:**

```rust
/*
═══════════════════════════════════════════════════════════════════════════════
 ФАЙЛ: src/module/filename.rs
═══════════════════════════════════════════════════════════════════════════════

📋 ПРИЗНАЧЕННЯ:
   [1-2 речення - що робить цей файл]

🎯 ВІДПОВІДАЛЬНІСТЬ:
   - [Функція 1]
   - [Функція 2]
   - [Функція 3]

🔗 ЗВ'ЯЗКИ З ІНШИМИ ФАЙЛАМИ:
   Імпортує:
   - [файл 1] - [навіщо]
   - [файл 2] - [навіщо]

   Експортує для:
   - [файл 1] - [що саме]

📦 ЗАЛЕЖНОСТІ:
   - [crate_name] = "version" - [навіщо використовується]

⚠️  ВАЖЛИВІ ОБМЕЖЕННЯ:
   1. [Обмеження 1]
   2. [Обмеження 2]

🧪 ТЕСТУВАННЯ:
   [Як перевірити що цей модуль працює]

📝 ПРИКЛАД ВИКОРИСТАННЯ:
   ```rust
   [Приклад коду]
   ```

🕐 ІСТОРІЯ:
   YYYY-MM-DD: [Створено/Змінено] - [причина]
   [Наступні зміни]

═══════════════════════════════════════════════════════════════════════════════
*/
```

**Критичні функції МАЮТЬ коментарі:**

```rust
/// Обчислює напрямок атаки на основі дельти миші
///
/// # Аргументи
/// * `mouse_delta` - Зміна позиції миші за останні N мс
/// * `threshold` - Мінімальний рух для реєстрації (dead zone)
///
/// # Повертає
/// `Direction` - один з 8 напрямків або `Center`
///
/// # Приклад
/// ```
/// let dir = calculate_attack_direction(Vec2::new(10.0, 5.0), 2.0);
/// assert_eq!(dir, Direction::TopRight);
/// ```
fn calculate_attack_direction(mouse_delta: Vec2, threshold: f32) -> Direction {
    // ...
}
```

### Комміт-повідомлення Git

**Формат:**
```
[Тип]: Короткий опис (до 50 символів)

Детальний опис:
- Що змінено
- Навіщо змінено
- Які є наслідки

[опційно] Closes #issue_number
```

**Типи:**
- `feat:` - нова функціональність
- `fix:` - виправлення бага
- `refactor:` - рефакторинг без зміни функціональності
- `docs:` - тільки документація
- `test:` - додавання/зміна тестів
- `chore:` - технічні зміни (dependencies, config)

**Приклад:**
```
feat: Add wgpu renderer with clear color

Детальний опис:
- Створено src/rendering/renderer.rs
- Додано ініціалізацію wgpu (device, queue, surface)
- Реалізовано clear екрану темно-синім кольором
- Інтегровано з main event loop

Closes #1
```

---

## 🔍 Принцип детермінізму для майбутнього netcode

**КРИТИЧНО:** З першого дня дотримуємось правил для rollback netcode:

### ❌ ЗАБОРОНЕНО в core/логіці:

1. **Float arithmetic:**
   ```rust
   ❌ let x: f32 = 1.0 / 3.0;  // Non-deterministic
   ✅ let x: I32F32 = I32F32::from_num(1) / I32F32::from_num(3);
   ```

2. **System time:**
   ```rust
   ❌ let now = SystemTime::now();
   ✅ let frame_time = game_state.frame * FIXED_TIMESTEP;
   ```

3. **Random без seed:**
   ```rust
   ❌ let damage = base + rand::random::<i32>() % 10;
   ✅ let damage = base + deterministic_hash(attacker_id, frame) % 10;
   ```

4. **HashMap/HashSet iteration без сортування:**
   ```rust
   ❌ for entity in entities.iter() { }
   ✅ let mut sorted: Vec<_> = entities.iter().collect();
      sorted.sort_by_key(|e| e.id);
      for entity in sorted { }
   ```

### ✅ ДОЗВОЛЕНО:

- **Rendering** - може бути недетермінованим (це лише візуал)
- **Audio** - може мати варіації
- **UI** - може використовувати system time
- **Debug tools** - будь-що

**Правило поділу:**
```
src/
  core/          ← ДЕТЕРМІНОВАНА логіка (NO IO, NO random, NO system time)
  rendering/     ← Недетермінований візуал (можна все)
  audio/         ← Недетерміновані ефекти
  ui/            ← Недетермінований UI
```

---

## 🧪 Тестування та верифікація

### Перед комітом ЗАВЖДИ:

```bash
# 1. Перевірка компіляції
cargo check

# 2. Збірка
cargo build

# 3. Запуск
cargo run

# 4. Тести (якщо є)
cargo test

# 5. Clippy (linter)
cargo clippy

# 6. Форматування (якщо змінювали код)
cargo fmt
```

### Логування для debugging:

```rust
// Використовуємо log crate з рівнями:
log::error!("Critical error: {}", msg);   // Тільки критичні помилки
log::warn!("Warning: {}", msg);           // Попередження
log::info!("Info: {}", msg);              // Важлива інформація
log::debug!("Debug: {}", msg);            // Детальна інформація (RUST_LOG=debug)
log::trace!("Trace: {}", msg);            // Дуже детально (RUST_LOG=trace)
```

**Запуск з логуванням:**
```bash
RUST_LOG=info cargo run    # Показати info і вище
RUST_LOG=debug cargo run   # Показати debug і вище
RUST_LOG=trace cargo run   # Показати все
```

---

## 🎯 Оптимальний workflow сесії

### Крок 1: Читання (5-10 хв)
```
1. Прочитати README.md
2. Прочитати PROGRESS.md - секцію "Наступні кроки"
3. Перевірити git log (останні коміти)
```

### Крок 2: Планування (2-5 хв)
```
1. Визначити 1-3 конкретні завдання для сесії
2. Оцінити чи є блокери
3. Створити TODO list (можна в PROGRESS.md)
```

### Крок 3: Робота (основний час)
```
1. Почати з першого завдання
2. Написати код + коментарі
3. Перевірити що працює (cargo run)
4. Закомітити (невеликі частини)
5. Оновити PROGRESS.md
6. Повторити для наступного завдання
```

### Крок 4: Завершення (5-10 хв)
```
1. Фінальна перевірка: cargo build && cargo run
2. Записати ВСЕ в PROGRESS.md (що зроблено)
3. Записати "Наступні кроки" для наступної сесії
4. Фінальний коміт
5. (Опційно) Push на GitHub
```

---

## 📊 Метрики якості документації

**Наступний AI має відповісти "Так" на ці питання:**

- [ ] Чи можу я зрозуміти стан проекту за 10 хвилин читання?
- [ ] Чи знаю я що робити далі без додаткових питань?
- [ ] Чи зрозумію я НАВІЩО був прийнятий той чи інший підхід?
- [ ] Чи зможу я запустити проект без помилок?
- [ ] Чи є у мене список відкритих проблем/питань?

**Якщо хоч на одне "Ні" → документація НЕПОВНА**

---

## 🛡️ Правила для AI-асистента

### Заборонено без явної вказівки користувача:

1. **Видаляти існуючий функціонал** - навіть якщо він здається непотрібним
2. **Додавати новий функціонал** - окрім того що явно запитано
3. **Змінювати архітектуру** - без обговорення та згоди
4. **"Покращувати" код** - рефакторинг тільки за запитом

### Завжди робити:

1. **Документувати зміни** - що і навіщо змінено
2. **Записувати рішення проблем** - в CLAUDE.md секція Common Issues
3. **Перевіряти перед видаленням** - grep для пошуку всіх використань

> **Принцип:** AI-асистент виконує тільки те, що явно запитано. Ніяких "покращень" з власної ініціативи.

---

## 🚨 Антипаттерни (ЧОГО НЕ РОБИТИ)

### ❌ Погано:
```markdown
Сесія 5: Додав рендер
- Зробив рендер
- Працює
```
**Проблема:** Наступний AI не знає ЯК, ЩО, НАВІЩО

### ✅ Добре:
```markdown
### 2025-12-14 (Сесія 5): Додано wgpu renderer з clear color
**Тривалість:** ~1.5 години

#### Виконано:
- ✅ Створено `src/rendering/renderer.rs`:
  - Struct `WgpuRenderer` з полями: device, queue, surface, config
  - Метод `new()` - ініціалізація wgpu
  - Метод `render()` - очищення екрану кольором (0.1, 0.2, 0.3, 1.0)
- ✅ Інтегровано в `main.rs`:
  - Створюється при resumed()
  - Викликається в RedrawRequested
- ✅ Перевірено: Вікно показує темно-синій екран ✅

#### Технічні деталі:
- wgpu backend: автовибір (Vulkan/DX12/Metal)
- Present mode: Fifo (VSync)
- Format: bgra8unorm
```

---

## 🔄 Оновлення цієї методології

**Коли оновлювати:**
- Знайдено кращий підхід до документування
- Виникла проблема через недостатню документацію
- Додано новий стандарт/правило

**Як оновлювати:**
1. Додати секцію з новим правилом
2. Оновити версію документу
3. Записати в PROGRESS.md що змінено в методології

---

## 📖 Управління дослідженнями

### Принцип
> **Перед початком нового дослідження - перевір чи воно вже є!**

### Файлова структура досліджень

```
docs/
├── RESEARCH_INDEX.md          # Головний індекс всіх досліджень
├── HUMANOID_ANIMATION_RESEARCH.md
├── ACTIVE_RAGDOLL_RESEARCH.md
├── SKELETAL_ANIMATION_SYSTEM.md
├── 3D_SPACE_UNDERSTANDING.md
└── [TOPIC]_RESEARCH.md        # Нові дослідження
```

### Перед початком дослідження

1. **Перевір `docs/RESEARCH_INDEX.md`** - можливо, дослідження вже є
2. Якщо є - прочитай існуючий документ
3. Якщо немає - створи новий документ

### Шаблон нового дослідження

```markdown
# [Topic] Research
## [Subtitle]

**Date:** YYYY-MM-DD
**Status:** In Progress | Complete
**Applies to:** [relevant systems]

---

## Table of Contents
[sections]

---

## 1. Section Name
[content with code examples]

---

## Sources
- [Source Name](URL)

---

**Related Research:**
- [Link to related docs]

**Next Research Topics:**
- [Future investigations needed]
```

### Після завершення дослідження

1. **Оновити `docs/RESEARCH_INDEX.md`** - додати новий запис
2. **Оновити `QUICK_START.md`** - якщо дослідження критичне
3. **Додати cross-references** - посилання на пов'язані документи

### Категорії досліджень

| Категорія | Приклади |
|-----------|----------|
| Animation & Character | Скелетна анімація, ragdoll, IK |
| Combat & Gameplay | Атаки, хітбокси, AI |
| Technical | Rendering, networking, optimization |
| Tools | Blender, debuggers, profilers |

---

## 📚 Довідкові ресурси

### Для розробки:
- Rust Book: https://doc.rust-lang.org/book/
- wgpu Tutorial: https://sotrh.github.io/learn-wgpu/
- Bevy Cheatbook (для референсів ECS): https://bevy-cheatbook.github.io/

### Для netcode (майбутнє):
- GGRS (rollback): https://github.com/gschup/ggrs
- Rollback netcode explained: https://ki.infil.net/w02-netcode.html

### Arena Combat специфічні:
- GDD: `docs/arena_combat_gdd.md`
- Tech decisions: `tech_stack_decision.md`
- **Research Index: `docs/RESEARCH_INDEX.md`**

---

**Останнє оновлення:** 2025-12-24
**Автор:** AI Assistant (Claude)
**Статус:** Active / Living Document
